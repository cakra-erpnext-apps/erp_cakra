# Agents

Agent Fleet / Assistant app for ERP CMI — the in-ERP AI assistant (board, chat,
email, auto-reply) extracted from `erp` into its own Frappe app. Works on the
Expedition documents (Packing List, Shipping List, Expense Note) and the core
Sales Invoice (via `erpnext_custom`).
